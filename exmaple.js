/**
 * @param {number} num1 - The upper part of the fraction
 * @param {number} num2 - The bottom of the fraction
 * @return {number} - Result of division. 
 */
function divide(num1, num2) {
    return num1/num2;
}